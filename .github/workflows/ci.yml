name: Build

on: [push, pull_request]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        rocq_version: [ "9.0", "9.1", "9.2", dev ]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build with Rocq ${{ matrix.rocq_version }}
        uses: coq-community/docker-coq-action@v1
        with:
          opam_file: 'mltac2.opam'
          coq_version: ${{ matrix.rocq_version }}